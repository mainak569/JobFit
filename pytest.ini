[pytest]
DJANGO_SETTINGS_MODULE = jobfit_api.settings
python_files = test_*.py
# Whitenoise warns when STATIC_ROOT doesn't exist. Tests never run
# collectstatic and never serve static files, so the warning is only noise.
filterwarnings =
    ignore:No directory at:UserWarning
